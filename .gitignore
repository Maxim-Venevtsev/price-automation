# Ignore real data folders (they contain confidential files)
base/
incoming/
clean/
output/

# BUT keep the folder itself in the repo (empty)
!base/.gitkeep
!incoming/.gitkeep
!clean/.gitkeep
!output/.gitkeep

# Keep sample data for documentation / examples
sample_data/

# Python cache
__pycache__/
*.pyc

# PyInstaller build artifacts (exe build)
dist/
build/
*.spec

# Logs
logs/
!logs/.gitkeep
